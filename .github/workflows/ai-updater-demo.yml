name: AI Updater Workflow (for live demo)

on:
  push:
    branches:
      - workflow/update-proto
  workflow_dispatch:

jobs:
  call-ai-updater:
    uses: gabegottlob/viam-ai-updater/.github/workflows/ai-updater-demo.yml@main
    with:
        target_branch: workflow/update-proto
        sdk: python
    secrets:
        GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
